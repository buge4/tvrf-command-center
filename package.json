{
  "name": "tvrf-command-center",
  "version": "1.0.0",
  "description": "AI Army Command Center Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "npm cache clean --force && rm -rf node_modules package-lock.json pnpm-lock.yaml yarn.lock .npm && npm install --force --no-audit --no-fund && echo '✅ Build completed successfully at $(date)' && echo 'Dependencies installed: express cors'",
    "clean": "rm -rf node_modules package-lock.json pnpm-lock.yaml yarn.lock .npm",
    "rebuild": "npm run clean && npm install --force"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}